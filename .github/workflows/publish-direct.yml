name: Build & Publish carton (Direct Release Upload)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v1.2.3 or v1.2.3-beta.1)'
        required: true
        type: string

permissions:
  contents: write

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true
  NUGET_XMLDOC_MODE: skip
  APP_NAME: carton

jobs:
  metadata:
    name: Resolve metadata
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.meta.outputs.version }}
      channel: ${{ steps.meta.outputs.channel }}
      is_prerelease: ${{ steps.meta.outputs.is_prerelease }}
      tag: ${{ steps.meta.outputs.tag }}
      release_name: ${{ steps.meta.outputs.release_name }}
    steps:
      - name: Compute release metadata
        id: meta
        shell: bash
        run: |
          set -euo pipefail
          tag="${{ inputs.tag }}"

          if [[ -z "$tag" ]]; then
            echo "inputs.tag is required." >&2
            exit 1
          fi

          version="$tag"
          if [[ "$version" == v* ]]; then
            version="${version#v}"
          fi

          channel="release"
          is_prerelease="false"
          shopt -s nocasematch
          if [[ "$tag" == *-beta* ]] || [[ "$tag" == *-rc* ]] || [[ "$tag" == *-preview* ]] || [[ "$version" == *-beta* ]] || [[ "$version" == *-rc* ]] || [[ "$version" == *-preview* ]]; then
            channel="beta"
            is_prerelease="true"
          fi
          shopt -u nocasematch

          app_name="${APP_NAME:-carton}"
          release_name="${app_name} ${version}"

          {
            echo "version=${version}"
            echo "channel=${channel}"
            echo "is_prerelease=${is_prerelease}"
            echo "tag=${tag}"
            echo "release_name=${release_name}"
          } >> "$GITHUB_OUTPUT"

  prepare_release:
    name: Prepare GitHub release
    runs-on: ubuntu-latest
    needs: metadata
    steps:
      - name: Create or update GitHub release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.metadata.outputs.tag }}
          REPO: ${{ github.repository }}
          RELEASE_NAME: ${{ needs.metadata.outputs.release_name }}
          IS_PRERELEASE: ${{ needs.metadata.outputs.is_prerelease }}
          TARGET_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail

          args=("$TAG" "--repo" "$REPO" "--title" "$RELEASE_NAME")
          if [[ "$IS_PRERELEASE" == "true" ]]; then
            args+=("--prerelease")
          fi

          target_ref="$TARGET_SHA"
          if gh api "repos/$REPO/git/ref/heads/$TAG" >/dev/null 2>&1; then
            target_ref="$TAG"
          fi

          if gh release view "$TAG" --repo "$REPO" >/dev/null 2>&1; then
            gh release edit "${args[@]}"
          else
            gh release create "${args[@]}" --target "$target_ref" --notes ""
          fi

      - name: Clear existing release assets
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.metadata.outputs.tag }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail

          gh api "repos/$REPO/releases/tags/$TAG" --jq '.assets[].id' | while read -r asset_id; do
            [[ -n "$asset_id" ]] || continue
            gh api "repos/$REPO/releases/assets/$asset_id" --method DELETE
          done

  build:
    name: Build ${{ matrix.rid }}
    needs:
      - metadata
      - prepare_release
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            rid: win-x64
            mainExe: carton.exe
            icon: src/carton.GUI/Assets/carton_icon.ico
          - os: windows-latest
            rid: win-arm64
            mainExe: carton.exe
            icon: src/carton.GUI/Assets/carton_icon.ico
          - os: ubuntu-22.04
            rid: linux-x64
            mainExe: carton
            icon: src/carton.GUI/Assets/carton_icon.png
          - os: ubuntu-22.04
            rid: linux-arm64
            mainExe: carton
            icon: src/carton.GUI/Assets/carton_icon.png
    env:
      VERSION: ${{ needs.metadata.outputs.version }}
      CHANNEL: ${{ needs.metadata.outputs.channel }}
      TAG: ${{ needs.metadata.outputs.tag }}
      REPO: ${{ github.repository }}
    steps:
      - uses: actions/checkout@v6

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Install native dependencies (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y clang lld binutils-aarch64-linux-gnu gcc-aarch64-linux-gnu libc6-dev-arm64-cross zlib1g-dev libkrb5-dev pkg-config

      - name: Install Velopack CLI (Linux/macOS)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          dotnet tool update --global vpk --version 0.0.1298
          echo "$HOME/.dotnet/tools" >> "$GITHUB_PATH"

      - name: Install Velopack CLI (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          dotnet tool update --global vpk --version 0.0.1298
          "$env:USERPROFILE\.dotnet\tools" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Publish (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          publish_dir="$GITHUB_WORKSPACE/artifacts/publish/${{ matrix.rid }}"
          mkdir -p "$publish_dir"
          props=(
            -c Release
            -r ${{ matrix.rid }}
            -o "$publish_dir"
            /p:PublishAot=true
            /p:SelfContained=true
            /p:StripSymbols=true
            /p:DebugSymbols=false
            /p:DebugType=None
            /p:InvariantGlobalization=true
            /p:IncludeNativeLibrariesForSelfExtract=true
            /p:EnableCompressionInSingleFile=true
          )
          if [[ "${{ matrix.rid }}" == "linux-arm64" ]]; then
            props+=(/p:ObjCopyName=aarch64-linux-gnu-objcopy)
          fi
          dotnet publish src/carton.GUI/carton.GUI.csproj "${props[@]}"
          echo "PUBLISH_DIR=$publish_dir" >> "$GITHUB_ENV"

      - name: Publish (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $publishDir = "$env:GITHUB_WORKSPACE\artifacts\publish\${{ matrix.rid }}"
          New-Item -ItemType Directory -Path $publishDir -Force | Out-Null
          dotnet publish src\carton.GUI\carton.GUI.csproj `
            -c Release `
            -r ${{ matrix.rid }} `
            -o $publishDir `
            /p:PublishAot=true `
            /p:SelfContained=true `
            /p:StripSymbols=true `
            /p:DebugSymbols=false `
            /p:DebugType=None `
            /p:InvariantGlobalization=true `
            /p:IncludeNativeLibrariesForSelfExtract=true `
            /p:EnableCompressionInSingleFile=true
          "PUBLISH_DIR=$publishDir" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Create portable archive (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          pack_dir="$GITHUB_WORKSPACE/artifacts/pack/${{ matrix.rid }}-$CHANNEL"
          mkdir -p "$pack_dir"
          find "$PUBLISH_DIR" -name '*.pdb' -delete
          portable_name="${APP_NAME}-${VERSION}-${{ matrix.rid }}-portable.tar.gz"
          tar -C "$PUBLISH_DIR" -czf "$pack_dir/$portable_name" .
          echo "PACK_DIR=$pack_dir" >> "$GITHUB_ENV"

      - name: Create portable archive (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $packDir = "$env:GITHUB_WORKSPACE\artifacts\pack\${{ matrix.rid }}-$env:CHANNEL"
          New-Item -ItemType Directory -Path $packDir -Force | Out-Null
          Get-ChildItem -Path $env:PUBLISH_DIR -Filter '*.pdb' -Recurse | Remove-Item -Force
          $portableName = "$env:APP_NAME-$env:VERSION-${{ matrix.rid }}-portable.zip"
          Compress-Archive -Path "$env:PUBLISH_DIR\*" -DestinationPath "$packDir\$portableName" -Force
          "PACK_DIR=$packDir" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Package with Velopack (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          icon_path=$(realpath '${{ matrix.icon }}')
          package_channel='${{ matrix.rid }}-'$CHANNEL
          vpk pack \
            --packId "$APP_NAME" \
            --packVersion "$VERSION" \
            --channel "$package_channel" \
            --runtime ${{ matrix.rid }} \
            --packDir "$PUBLISH_DIR" \
            --mainExe '${{ matrix.mainExe }}' \
            --packTitle "$APP_NAME" \
            --icon "$icon_path" \
            --outputDir "$PACK_DIR"

      - name: Package with Velopack (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $iconPath = (Resolve-Path '${{ matrix.icon }}').Path
          $packageChannel = "${{ matrix.rid }}-$env:CHANNEL"
          vpk pack `
            --packId "$env:APP_NAME" `
            --packVersion $env:VERSION `
            --channel $packageChannel `
            --runtime ${{ matrix.rid }} `
            --packDir $env:PUBLISH_DIR `
            --mainExe '${{ matrix.mainExe }}' `
            --packTitle "$env:APP_NAME" `
            --icon $iconPath `
            --outputDir $env:PACK_DIR

      - name: Prepare release assets (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob

          release_dir="$RUNNER_TEMP/release-assets-${{ matrix.rid }}"
          rm -rf "$release_dir"
          mkdir -p "$release_dir"

          for setup in "$PACK_DIR"/*setup.exe; do
            [[ -f "$setup" ]] || continue
            cp -f "$setup" "$release_dir/${APP_NAME}-${VERSION}-${{ matrix.rid }}-Setup.exe"
          done

          for appimage in "$PACK_DIR"/*.AppImage; do
            [[ -f "$appimage" ]] || continue
            cp -f "$appimage" "$release_dir/${APP_NAME}-${VERSION}-${{ matrix.rid }}.AppImage"
          done

          for pattern in '*-portable.zip' '*-portable.tar.gz' '*.nupkg' 'releases*.json'; do
            for f in "$PACK_DIR"/$pattern; do
              [[ -f "$f" ]] || continue
              cp -f "$f" "$release_dir/$(basename "$f")"
            done
          done

          if ! find "$release_dir" -maxdepth 1 -type f | grep -q .; then
            echo "No release assets were prepared." >&2
            exit 1
          fi

          echo "RELEASE_ASSETS_DIR=$release_dir" >> "$GITHUB_ENV"

      - name: Prepare release assets (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $releaseDir = Join-Path $env:RUNNER_TEMP "release-assets-${{ matrix.rid }}"
          if (Test-Path $releaseDir) {
            Remove-Item -Recurse -Force $releaseDir
          }
          New-Item -ItemType Directory -Path $releaseDir -Force | Out-Null

          $setup = Get-ChildItem -Path $env:PACK_DIR -Filter '*setup.exe' -File | Select-Object -First 1
          if ($setup) {
            Copy-Item $setup.FullName (Join-Path $releaseDir "$env:APP_NAME-$env:VERSION-${{ matrix.rid }}-Setup.exe") -Force
          }

          $appImage = Get-ChildItem -Path $env:PACK_DIR -Filter '*.AppImage' -File | Select-Object -First 1
          if ($appImage) {
            Copy-Item $appImage.FullName (Join-Path $releaseDir "$env:APP_NAME-$env:VERSION-${{ matrix.rid }}.AppImage") -Force
          }

          foreach ($pattern in @('*-portable.zip', '*-portable.tar.gz', '*.nupkg', 'releases*.json')) {
            Get-ChildItem -Path $env:PACK_DIR -Filter $pattern -File | ForEach-Object {
              Copy-Item $_.FullName (Join-Path $releaseDir $_.Name) -Force
            }
          }

          $prepared = Get-ChildItem -Path $releaseDir -File
          if (-not $prepared) {
            Write-Error 'No release assets were prepared.'
          }

          "RELEASE_ASSETS_DIR=$releaseDir" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Upload release assets (Linux)
        if: runner.os == 'Linux'
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          shopt -s nullglob

          uploads=("$RELEASE_ASSETS_DIR"/*)
          if [[ ${#uploads[@]} -eq 0 ]]; then
            echo "No files to upload." >&2
            exit 1
          fi

          gh release upload "$TAG" "${uploads[@]}" --repo "$REPO" --clobber

      - name: Upload release assets (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $uploads = Get-ChildItem -Path $env:RELEASE_ASSETS_DIR -File | Sort-Object Name | Select-Object -ExpandProperty FullName
          if (-not $uploads) {
            Write-Error 'No files to upload.'
          }

          & gh release upload $env:TAG $uploads --repo $env:REPO --clobber

  finalize_release:
    name: Update release notes
    runs-on: ubuntu-latest
    needs:
      - metadata
      - build
    if: ${{ needs.build.result == 'success' }}
    steps:
      - name: Update release notes
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.metadata.outputs.tag }}
          REPO: ${{ github.repository }}
          VERSION: ${{ needs.metadata.outputs.version }}
          CHANNEL: ${{ needs.metadata.outputs.channel }}
          RELEASE_NAME: ${{ needs.metadata.outputs.release_name }}
          IS_PRERELEASE: ${{ needs.metadata.outputs.is_prerelease }}
        run: |
          set -euo pipefail

          app_name="${APP_NAME:-carton}"
          base_url="https://github.com/$REPO/releases/download/$TAG"
          notes_file="$RUNNER_TEMP/release-notes.md"

          cat > "$notes_file" <<EOF
          ## 下载

          如果你不确定该下载哪个版本，Windows 用户优先选择 \`win-x64\` 安装版。

          - Windows x64 安装版: [${app_name}-${VERSION}-win-x64-Setup.exe](${base_url}/${app_name}-${VERSION}-win-x64-Setup.exe)
          - Windows x64 绿色版: [${app_name}-${VERSION}-win-x64-portable.zip](${base_url}/${app_name}-${VERSION}-win-x64-portable.zip)
          - Windows ARM64 安装版: [${app_name}-${VERSION}-win-arm64-Setup.exe](${base_url}/${app_name}-${VERSION}-win-arm64-Setup.exe)
          - Windows ARM64 绿色版: [${app_name}-${VERSION}-win-arm64-portable.zip](${base_url}/${app_name}-${VERSION}-win-arm64-portable.zip)
          - Linux x64 AppImage: [${app_name}-${VERSION}-linux-x64.AppImage](${base_url}/${app_name}-${VERSION}-linux-x64.AppImage)
          - Linux x64 绿色版: [${app_name}-${VERSION}-linux-x64-portable.tar.gz](${base_url}/${app_name}-${VERSION}-linux-x64-portable.tar.gz)
          - Linux ARM64 AppImage: [${app_name}-${VERSION}-linux-arm64.AppImage](${base_url}/${app_name}-${VERSION}-linux-arm64.AppImage)
          - Linux ARM64 绿色版: [${app_name}-${VERSION}-linux-arm64-portable.tar.gz](${base_url}/${app_name}-${VERSION}-linux-arm64-portable.tar.gz)

          ## 说明

          - 当前发布通道：\`${CHANNEL}\`
          - \`.nupkg\` 和 \`releases*.json\` 文件供自动更新使用，普通用户一般不需要手动下载。
          EOF

          gh release edit "$TAG" --repo "$REPO" --notes-file "$notes_file"
